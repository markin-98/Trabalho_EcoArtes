<main>
<html lang="pt-BR"></html>
    <div class="container">
        <div class="row">
            <div class=" col-6 offset-3">
                <form action="/editar" method="post" enctype="multipart/form-data">

                    <input type="hidden" name="nomeImagem" value="{{produto.imagem}}">

                    <img src="/imagens/{{produto.imagem}}">
                    <input type="hidden" name="codigo" value="{{produto.codigo}}">
                    <input type="text" name="nome" value="{{produto.nome}}" class="form-control"
                        placeholder="Nome do produto">
                    <input type="number" step="0.01" name="valor" value="{{produto.valor}}" class="form-control"
                        placeholder="Valor do produto">
                    <input type="text" name="descricao" value="{{produto.descricao}}" class="form-control"
                        placeholder="Descrição do produto">
                    <input type="file" name="imagem" class="form-control">
                    <input type="submit" class="btn btn-primary" value="Alterar">
                    <a href="/listaprodutos" class="btn btn-secondary">Cancelar</a>
                </form>
            </div>
        </div>
    </div>

</main>